<div class="profile">
  <% flash.each do |key, value| %>
    <%= content_tag(:div, value, class: "#{key}") %>
  <% end %>
  <h1><%= @user.name %></h1>
  <p>メールアドレス </p>
  <p><%= @user.email %></p>
</div>
